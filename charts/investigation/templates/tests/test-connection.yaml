apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "investigation.fullname" . }}-test-connection"
  labels:
    {{- include "investigation.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "investigation.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
