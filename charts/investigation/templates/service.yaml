apiVersion: v1
kind: Service
metadata:
  name: {{ include "investigation.fullname" . }}
  labels:
    {{- include "investigation.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "investigation.selectorLabels" . | nindent 4 }}
